name: build

on:
  pull_request:
    branches: master
  push:
    branches: master
    tags:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Set up Docker Buildx
        id: buildx
        uses: crazy-max/ghaction-docker-buildx@v1
        with:
          version: latest
      - name: Available platforms
        run: echo ${{ steps.buildx.outputs.platforms }}
      - name: Login to Docker Registry
        run: echo $DOCKER_PASSWORD | docker login --username $DOCKER_USERNAME --password-stdin
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      - name: Run Buildx
        run: |
          docker buildx build \
            --platform linux/amd64,linux/arm/v7 \
            -t mlandauer/home-dashboard:$GITHUB_SHA \
            --push \
            .
      - name: Deploy
        run: |
          wget https://downloads.deviceplane.com/cli/latest/linux/amd64/deviceplane
          chmod +x deviceplane
          ./deviceplane deploy home-dashboard deviceplane.yml
        env:
          DEVICEPLANE_ACCESS_KEY: ${{ secrets.DEVICEPLANE_ACCESS_KEY }}
          DEVICEPLANE_PROJECT: ${{ secrets.DEVICEPLANE_PROJECT }}
          INFLUXDB_URL: ${{ secrets.INFLUXDB_URL }}
          INFLUXDB_TOKEN: ${{ secrets.INFLUXDB_TOKEN }}
          INFLUXDB_BUCKET: ${{ secrets.INFLUXDB_BUCKET }}
          INFLUXDB_ORG: ${{ secrets.INFLUXDB_ORG }}
